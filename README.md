# mask_stats
Python3 script to evaluate fraction of masked pixels in an ALMA cube

The script is run as "local_path/mask_stats.py <input_data_set> <output_data_set>"

The file format used is FITS.

The first line in the script must then be changed to point to your local python3 installation and the script must be made executable with "chmod +x trim_uvfits.py"

Alternatively one can run the script as "python3 local_path/mask_stats.py <input_data_set> <output_data_set>"
